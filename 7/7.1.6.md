# 柒.1.6 命令模式

命令模式是一种行为型设计模式，它将请求封装成对象，从而让你可以用不同的请求对客户进行参数化，对请求排队或记录请求日志，以及支持可撤销的操作。

## 柒.1.6.1 命令模式的基本概念

### 核心思想

命令模式的核心思想是将"请求"封装成对象，这样可以用不同的请求对客户进行参数化，对请求排队或记录请求日志，以及支持可撤销的操作。

### 主要角色

1. **Command（命令接口）**：声明执行操作的接口
2. **ConcreteCommand（具体命令）**：实现命令接口，绑定接收者和动作
3. **Receiver（接收者）**：知道如何实施与执行一个请求相关的操作
4. **Invoker（调用者）**：要求命令执行这个请求
5. **Client（客户端）**：创建具体命令对象并设定它的接收者

## 柒.1.6.2 命令模式的结构

```javascript
// 命令接口
class Command {
  execute() {
    throw new Error('子类必须实现execute方法');
  }
  
  undo() {
    throw new Error('子类必须实现undo方法');
  }
}

// 具体命令
class ConcreteCommand extends Command {
  constructor(receiver) {
    super();
    this.receiver = receiver;
  }
  
  execute() {
    this.receiver.action();
  }
  
  undo() {
    this.receiver.undoAction();
  }
}

// 接收者
class Receiver {
  action() {
    console.log('执行具体操作');
  }
  
  undoAction() {
    console.log('撤销操作');
  }
}

// 调用者
class Invoker {
  constructor() {
    this.commands = [];
  }
  
  setCommand(command) {
    this.commands.push(command);
  }
  
  executeCommands() {
    this.commands.forEach(command => command.execute());
  }
  
  undoLastCommand() {
    if (this.commands.length > 0) {
      const lastCommand = this.commands.pop();
      lastCommand.undo();
    }
  }
}

// 客户端
const receiver = new Receiver();
const command = new ConcreteCommand(receiver);
const invoker = new Invoker();

invoker.setCommand(command);
invoker.executeCommands();
invoker.undoLastCommand();
```

## 柒.1.6.3 实际应用示例

### 1. 文本编辑器

```javascript
// 文本编辑器命令模式实现
class TextEditor {
  constructor() {
    this.content = '';
    this.history = [];
    this.currentIndex = -1;
  }
  
  write(text) {
    this.content += text;
    console.log(`写入文本: ${text}`);
    console.log(`当前内容: ${this.content}`);
  }
  
  delete(count) {
    const deletedText = this.content.slice(-count);
    this.content = this.content.slice(0, -count);
    console.log(`删除文本: ${deletedText}`);
    console.log(`当前内容: ${this.content}`);
    return deletedText;
  }
  
  getContent() {
    return this.content;
  }
}

// 命令基类
class EditorCommand {
  constructor(editor) {
    this.editor = editor;
  }
  
  execute() {
    throw new Error('子类必须实现execute方法');
  }
  
  undo() {
    throw new Error('子类必须实现undo方法');
  }
}

// 写入命令
class WriteCommand extends EditorCommand {
  constructor(editor, text) {
    super(editor);
    this.text = text;
  }
  
  execute() {
    this.editor.write(this.text);
  }
  
  undo() {
    this.editor.delete(this.text.length);
  }
}

// 删除命令
class DeleteCommand extends EditorCommand {
  constructor(editor, count) {
    super(editor);
    this.count = count;
    this.deletedText = '';
  }
  
  execute() {
    this.deletedText = this.editor.delete(this.count);
  }
  
  undo() {
    this.editor.write(this.deletedText);
  }
}

// 命令管理器
class CommandManager {
  constructor() {
    this.commands = [];
    this.currentIndex = -1;
  }
  
  executeCommand(command) {
    // 执行命令
    command.execute();
    
    // 添加到历史记录
    this.commands.splice(this.currentIndex + 1);
    this.commands.push(command);
    this.currentIndex++;
  }
  
  undo() {
    if (this.currentIndex >= 0) {
      const command = this.commands[this.currentIndex];
      command.undo();
      this.currentIndex--;
      return true;
    }
    return false;
  }
  
  redo() {
    if (this.currentIndex < this.commands.length - 1) {
      this.currentIndex++;
      const command = this.commands[this.currentIndex];
      command.execute();
      return true;
    }
    return false;
  }
}

// 使用示例
const editor = new TextEditor();
const commandManager = new CommandManager();

// 执行一系列命令
commandManager.executeCommand(new WriteCommand(editor, 'Hello'));
commandManager.executeCommand(new WriteCommand(editor, ' World'));
commandManager.executeCommand(new DeleteCommand(editor, 5));

console.log('\n--- 撤销操作 ---');
commandManager.undo();
commandManager.undo();

console.log('\n--- 重做操作 ---');
commandManager.redo();
commandManager.redo();
```

### 2. 智能家居系统

```javascript
// 智能家居设备
class Light {
  constructor(location) {
    this.location = location;
    this.isOn = false;
  }
  
  turnOn() {
    this.isOn = true;
    console.log(`${this.location}的灯已打开`);
  }
  
  turnOff() {
    this.isOn = false;
    console.log(`${this.location}的灯已关闭`);
  }
  
  getStatus() {
    return this.isOn;
  }
}

class Fan {
  constructor(location) {
    this.location = location;
    this.speed = 0;
  }
  
  setSpeed(speed) {
    this.speed = speed;
    console.log(`${this.location}的风扇速度设置为${speed}`);
  }
  
  turnOff() {
    this.speed = 0;
    console.log(`${this.location}的风扇已关闭`);
  }
  
  getSpeed() {
    return this.speed;
  }
}

// 命令接口
class HomeCommand {
  execute() {
    throw new Error('子类必须实现execute方法');
  }
  
  undo() {
    throw new Error('子类必须实现undo方法');
  }
}

// 灯光命令
class LightCommand extends HomeCommand {
  constructor(light) {
    super();
    this.light = light;
    this.previousState = light.getStatus();
  }
  
  execute() {
    if (this.light.getStatus()) {
      this.light.turnOff();
    } else {
      this.light.turnOn();
    }
  }
  
  undo() {
    if (this.previousState) {
      this.light.turnOn();
    } else {
      this.light.turnOff();
    }
  }
}

// 风扇命令
class FanCommand extends HomeCommand {
  constructor(fan, speed) {
    super();
    this.fan = fan;
    this.speed = speed;
    this.previousSpeed = fan.getSpeed();
  }
  
  execute() {
    this.fan.setSpeed(this.speed);
  }
  
  undo() {
    this.fan.setSpeed(this.previousSpeed);
  }
}

// 宏命令（组合命令）
class MacroCommand extends HomeCommand {
  constructor(commands) {
    super();
    this.commands = commands;
  }
  
  execute() {
    this.commands.forEach(command => command.execute());
  }
  
  undo() {
    // 反向执行撤销操作
    for (let i = this.commands.length - 1; i >= 0; i--) {
      this.commands[i].undo();
    }
  }
}

// 使用示例
const livingRoomLight = new Light('客厅');
const bedroomLight = new Light('卧室');
const livingRoomFan = new Fan('客厅');

const lightCommands = [
  new LightCommand(livingRoomLight),
  new LightCommand(bedroomLight)
];

const fanCommand = new FanCommand(livingRoomFan, 3);

// 创建宏命令
const macroCommand = new MacroCommand([
  ...lightCommands,
  fanCommand
]);

console.log('执行宏命令：');
macroCommand.execute();

console.log('\n撤销宏命令：');
macroCommand.undo();
```

## 柒.1.6.4 命令模式的优缺点

### 优点

1. **解耦**：将请求发送者和接收者解耦
2. **可扩展**：容易扩展新的命令
3. **组合**：可以组合多个命令形成宏命令
4. **撤销**：容易实现撤销和重做功能
5. **队列**：可以将命令放入队列中执行

### 缺点

1. **复杂性**：可能产生过多的命令类
2. **内存占用**：需要保存命令历史记录
3. **调试困难**：命令的执行流程可能比较复杂

## 柒.1.6.5 适用场景

1. **需要撤销/重做功能**：如文本编辑器、绘图软件
2. **需要记录操作日志**：如系统操作记录
3. **需要支持宏命令**：如批处理操作
4. **需要队列化请求**：如任务调度系统
5. **需要支持事务操作**：如数据库操作

## 柒.1.6.6 结语

命令模式是一种非常实用的设计模式，特别适合需要支持撤销、重做、日志记录等功能的场景。在JavaScript中，虽然语言本身比较灵活，但命令模式仍然能够帮助我们构建更加清晰和可维护的代码结构。

通过合理使用命令模式，你可以：
- 构建更加灵活的命令系统
- 实现复杂的撤销/重做功能
- 支持宏命令和批处理操作
- 提高代码的可扩展性和可维护性