# 叁.2.1 React Hooks究竟是什么？

React Hooks是React 16.8版本引入的新特性，它让函数组件能够使用状态（state）和其他React特性，而无需编写class组件。Hooks的出现彻底改变了React组件的编写方式，让函数组件变得更加强大和灵活。

## 叁.2.1.1 为什么需要Hooks？

### Class组件的问题

在Hooks出现之前，React主要使用class组件来管理状态和生命周期。但是class组件存在以下问题：

1. **组件之间难以复用状态逻辑**：高阶组件和render props等模式让组件变得复杂
2. **复杂组件变得难以理解**：生命周期方法中经常包含不相关的逻辑
3. **this绑定问题**：在JavaScript中，class的方法默认不会绑定this，容易出错
4. **代码组织困难**：相关的逻辑分散在不同的生命周期方法中

### Hooks的优势

Hooks解决了这些问题：

- **更好的逻辑复用**：可以在多个组件之间复用状态逻辑
- **更清晰的代码组织**：相关的逻辑可以组织在一起
- **避免this绑定问题**：函数组件没有this，避免了绑定问题
- **更容易测试**：函数组件更容易进行单元测试

## 叁.2.1.2 常用的Hooks

### 1. useState

`useState`是最基本的Hook，用于在函数组件中添加状态：

```javascript
import React, { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);
  
  return (
    <div>
      <p>你点击了 {count} 次</p>
      <button onClick={() => setCount(count + 1)}>
        点击我
      </button>
    </div>
  );
}
```

### 2. useEffect

`useEffect`用于处理副作用，相当于class组件中的`componentDidMount`、`componentDidUpdate`和`componentWillUnmount`：

```javascript
import React, { useState, useEffect } from 'react';

function Example() {
  const [count, setCount] = useState(0);

  // 相当于 componentDidMount 和 componentDidUpdate
  useEffect(() => {
    document.title = `你点击了 ${count} 次`;
  });

  // 只在组件挂载时执行一次
  useEffect(() => {
    console.log('组件已挂载');
  }, []);

  // 当count变化时执行
  useEffect(() => {
    console.log(`count变化为: ${count}`);
  }, [count]);

  return (
    <div>
      <p>你点击了 {count} 次</p>
      <button onClick={() => setCount(count + 1)}>
        点击我
      </button>
    </div>
  );
}
```

### 3. useContext

`useContext`用于在函数组件中使用Context：

```javascript
import React, { createContext, useContext } from 'react';

const ThemeContext = createContext('light');

function ThemedButton() {
  const theme = useContext(ThemeContext);
  return <button className={theme}>按钮</button>;
}

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <ThemedButton />
    </ThemeContext.Provider>
  );
}
```

### 4. useReducer

`useReducer`是`useState`的替代方案，适用于复杂的状态逻辑：

```javascript
import React, { useReducer } from 'react';

const initialState = { count: 0 };

function reducer(state, action) {
  switch (action.type) {
    case 'increment':
      return { count: state.count + 1 };
    case 'decrement':
      return { count: state.count - 1 };
    case 'reset':
      return { count: 0 };
    default:
      throw new Error();
  }
}

function Counter() {
  const [state, dispatch] = useReducer(reducer, initialState);

  return (
    <div>
      Count: {state.count}
      <button onClick={() => dispatch({ type: 'increment' })}>+</button>
      <button onClick={() => dispatch({ type: 'decrement' })}>-</button>
      <button onClick={() => dispatch({ type: 'reset' })}>重置</button>
    </div>
  );
}
```

### 5. useCallback和useMemo

`useCallback`和`useMemo`用于性能优化：

```javascript
import React, { useState, useCallback, useMemo } from 'react';

function ExpensiveComponent({ onButtonClick, data }) {
  // 使用useMemo缓存计算结果
  const expensiveValue = useMemo(() => {
    return data.reduce((sum, item) => sum + item.value, 0);
  }, [data]);

  return (
    <div>
      <p>计算结果: {expensiveValue}</p>
      <button onClick={onButtonClick}>点击</button>
    </div>
  );
}

function ParentComponent() {
  const [count, setCount] = useState(0);
  const [data] = useState([{ value: 1 }, { value: 2 }, { value: 3 }]);

  // 使用useCallback缓存函数
  const handleButtonClick = useCallback(() => {
    setCount(count + 1);
  }, [count]);

  return (
    <div>
      <p>Count: {count}</p>
      <ExpensiveComponent 
        onButtonClick={handleButtonClick} 
        data={data} 
      />
    </div>
  );
}
```

## 叁.2.1.3 自定义Hooks

自定义Hooks是Hooks最强大的特性之一，它让你可以提取组件逻辑到可重用的函数中：

```javascript
import { useState, useEffect } from 'react';

// 自定义Hook：使用窗口大小
function useWindowSize() {
  const [windowSize, setWindowSize] = useState({
    width: window.innerWidth,
    height: window.innerHeight,
  });

  useEffect(() => {
    function handleResize() {
      setWindowSize({
        width: window.innerWidth,
        height: window.innerHeight,
      });
    }

    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  return windowSize;
}

// 自定义Hook：使用本地存储
function useLocalStorage(key, initialValue) {
  const [storedValue, setStoredValue] = useState(() => {
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch (error) {
      console.log(error);
      return initialValue;
    }
  });

  const setValue = value => {
    try {
      setStoredValue(value);
      window.localStorage.setItem(key, JSON.stringify(value));
    } catch (error) {
      console.log(error);
    }
  };

  return [storedValue, setValue];
}

// 使用自定义Hooks
function MyComponent() {
  const windowSize = useWindowSize();
  const [name, setName] = useLocalStorage('name', '张三');

  return (
    <div>
      <p>窗口大小: {windowSize.width} x {windowSize.height}</p>
      <input
        value={name}
        onChange={e => setName(e.target.value)}
        placeholder="输入你的名字"
      />
    </div>
  );
}
```

## 叁.2.1.4 Hooks的使用规则

使用Hooks时必须遵循以下规则：

### 1. 只在最顶层调用Hooks

不要在循环、条件或嵌套函数中调用Hooks：

```javascript
// ❌ 错误：在条件语句中调用Hook
if (condition) {
  useEffect(() => {});
}

// ❌ 错误：在循环中调用Hook
for (let i = 0; i < 10; i++) {
  useState();
}

// ✅ 正确：在组件顶层调用Hook
function MyComponent() {
  const [count, setCount] = useState(0);
  
  if (count > 0) {
    // 这里可以写其他逻辑，但不能调用Hook
    console.log('count大于0');
  }
  
  useEffect(() => {
    // Hook调用在顶层
  });
}
```

### 2. 只在React函数中调用Hooks

不要在普通的JavaScript函数中调用Hooks：

```javascript
// ❌ 错误：在普通函数中调用Hook
function regularFunction() {
  const [state, setState] = useState(0);
}

// ✅ 正确：在React函数组件中调用Hook
function MyComponent() {
  const [state, setState] = useState(0);
}

// ✅ 正确：在自定义Hook中调用Hook
function useCustomHook() {
  const [state, setState] = useState(0);
  return [state, setState];
}
```

## 叁.2.1.5 Hooks的最佳实践

### 1. 合理使用依赖数组

```javascript
// 避免无限循环
useEffect(() => {
  // 这个effect会在每次渲染后执行
  console.log('每次渲染都执行');
});

// 只在组件挂载时执行
useEffect(() => {
  console.log('只在挂载时执行');
}, []);

// 当特定值变化时执行
useEffect(() => {
  console.log('当count变化时执行');
}, [count]);
```

### 2. 使用函数式更新

```javascript
// 避免依赖过期的值
const [count, setCount] = useState(0);

// ❌ 可能有问题
useEffect(() => {
  const timer = setInterval(() => {
    setCount(count + 1); // 这里的count可能是过期的
  }, 1000);
  return () => clearInterval(timer);
}, [count]);

// ✅ 使用函数式更新
useEffect(() => {
  const timer = setInterval(() => {
    setCount(prevCount => prevCount + 1); // 总是使用最新的值
  }, 1000);
  return () => clearInterval(timer);
}, []); // 不需要依赖count
```

### 3. 合理拆分Hooks

```javascript
// ❌ 一个Hook做太多事情
function useUserData() {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);
  
  // 用户数据逻辑
  // 加载状态逻辑
  // 错误处理逻辑
  
  return { user, loading, error };
}

// ✅ 拆分成多个专门的Hooks
function useUser() {
  const [user, setUser] = useState(null);
  // 用户相关逻辑
  return user;
}

function useLoading() {
  const [loading, setLoading] = useState(false);
  // 加载状态逻辑
  return loading;
}

function useError() {
  const [error, setError] = useState(null);
  // 错误处理逻辑
  return error;
}
```

## 叁.2.1.6 结语

React Hooks的出现让React函数组件变得更加强大和灵活。它们解决了class组件的许多问题，让代码更加清晰和易于维护。通过合理使用Hooks，你可以：

- 更好地组织组件逻辑
- 更容易地复用状态逻辑
- 编写更简洁的组件代码
- 避免常见的React陷阱

虽然Hooks需要一些时间来适应，但一旦掌握，它们将成为你React开发工具箱中不可或缺的一部分。

## 参考文献

{% hint style="info" %}
[React Hooks 新手筆記](https://medium.com/@z3388638/react-hooks-%E6%96%B0%E6%89%8B%E7%AD%86%E8%A8%98-8c9f1cccd142)
{% endhint %}

